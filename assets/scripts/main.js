/**
 * @fileoverview Module entry point that re-exports the composed LinePack Pro
 * runtime for direct consumption by the browser bundle.
 */

import { LinePackPro } from './layers/app.js';

export default LinePackPro;
